spring:
  data:
    redis:
      host: xxxxxx
      port: 59000
      password: ${REDIS_PASSWORD}
      ttl: 3600
      timeout: 30000

pgvector:
  database: dp
  host: xxxxx
  port : 54328
  user: root
  password: ${PGVECTOR_PASSWORD}
  table: agent_embedding


rag:
  docs-path: /data/docs