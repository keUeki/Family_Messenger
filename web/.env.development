# Leave empty to use the Vite proxy (same-origin /api/**); no CORS setup is needed in development
VITE_API_BASE=
# Leave empty to use the Vite proxy (same-origin /ws/netty) and ignore the LAN address the backend hands out
VITE_WS_BASE=/ws/netty
